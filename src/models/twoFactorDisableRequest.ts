/**
 * FusionAuth APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema';
import { EventInfo, eventInfoSchema } from './eventInfo';

export interface TwoFactorDisableRequest {
  applicationId?: string;
  code?: string;
  methodId?: string;
  /** Information about a user event (login, register, etc) that helps identify the source of the event (location, device type, OS, etc). */
  eventInfo?: EventInfo;
}

export const twoFactorDisableRequestSchema: Schema<TwoFactorDisableRequest> = object(
  {
    applicationId: ['applicationId', optional(string())],
    code: ['code', optional(string())],
    methodId: ['methodId', optional(string())],
    eventInfo: ['eventInfo', optional(lazy(() => eventInfoSchema))],
  }
);
