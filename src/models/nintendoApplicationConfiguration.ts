/**
 * FusionAuth APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  dict,
  object,
  optional,
  Schema,
  string,
  unknown,
} from '../schema';

export interface NintendoApplicationConfiguration {
  buttonText?: string;
  clientId?: string;
  clientSecret?: string;
  emailClaim?: string;
  scope?: string;
  uniqueIdClaim?: string;
  usernameClaim?: string;
  data?: Record<string, unknown>;
  createRegistration?: boolean;
}

export const nintendoApplicationConfigurationSchema: Schema<NintendoApplicationConfiguration> = object(
  {
    buttonText: ['buttonText', optional(string())],
    clientId: ['client_id', optional(string())],
    clientSecret: ['client_secret', optional(string())],
    emailClaim: ['emailClaim', optional(string())],
    scope: ['scope', optional(string())],
    uniqueIdClaim: ['uniqueIdClaim', optional(string())],
    usernameClaim: ['usernameClaim', optional(string())],
    data: ['data', optional(dict(unknown()))],
    createRegistration: ['createRegistration', optional(boolean())],
  }
);
