/**
 * FusionAuth APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  bigint,
  dict,
  lazy,
  object,
  optional,
  Schema,
  string,
  unknown,
} from '../schema';
import { Entity, entitySchema } from './entity';

/** A grant for an entity to a user or another entity. */
export interface EntityGrant {
  data?: Record<string, unknown>;
  /** Models an entity that a user can be granted permissions to. Or an entity that can be granted permissions to another entity. */
  entity?: Entity;
  id?: string;
  /** The number of milliseconds since the unix epoch: January 1, 1970 00:00:00 UTC. This value is always in UTC. */
  insertInstant?: bigint;
  /** The number of milliseconds since the unix epoch: January 1, 1970 00:00:00 UTC. This value is always in UTC. */
  lastUpdateInstant?: bigint;
  permissions?: unknown[];
  recipientEntityId?: string;
  userId?: string;
}

export const entityGrantSchema: Schema<EntityGrant> = object({
  data: ['data', optional(dict(unknown()))],
  entity: ['entity', optional(lazy(() => entitySchema))],
  id: ['id', optional(string())],
  insertInstant: ['insertInstant', optional(bigint())],
  lastUpdateInstant: ['lastUpdateInstant', optional(bigint())],
  permissions: ['permissions', optional(array(unknown()))],
  recipientEntityId: ['recipientEntityId', optional(string())],
  userId: ['userId', optional(string())],
});
