/**
 * FusionAuth APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, optional, Schema, string } from '../schema';

/** API response for refreshing a JWT with a Refresh Token.  <p>  Using a different response object from RefreshTokenResponse because the retrieve response will return an object for refreshToken, and this is a  string. */
export interface JWTRefreshResponse {
  refreshToken?: string;
  refreshTokenId?: string;
  token?: string;
}

export const jWTRefreshResponseSchema: Schema<JWTRefreshResponse> = object({
  refreshToken: ['refreshToken', optional(string())],
  refreshTokenId: ['refreshTokenId', optional(string())],
  token: ['token', optional(string())],
});
