/**
 * FusionAuth APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { boolean, lazy, object, optional, Schema } from '../schema';
import { EventInfo, eventInfoSchema } from './eventInfo';

/** Request for the Tenant API to delete a tenant rather than using the URL parameters. */
export interface TenantDeleteRequest {
  async?: boolean;
  /** Information about a user event (login, register, etc) that helps identify the source of the event (location, device type, OS, etc). */
  eventInfo?: EventInfo;
}

export const tenantDeleteRequestSchema: Schema<TenantDeleteRequest> = object({
  async: ['async', optional(boolean())],
  eventInfo: ['eventInfo', optional(lazy(() => eventInfoSchema))],
});
