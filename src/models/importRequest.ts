/**
 * FusionAuth APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  number,
  object,
  optional,
  Schema,
  string,
} from '../schema';
import { EventInfo, eventInfoSchema } from './eventInfo';
import { User, userSchema } from './user';

/** Import request. */
export interface ImportRequest {
  encryptionScheme?: string;
  factor?: number;
  users?: User[];
  validateDbConstraints?: boolean;
  /** Information about a user event (login, register, etc) that helps identify the source of the event (location, device type, OS, etc). */
  eventInfo?: EventInfo;
}

export const importRequestSchema: Schema<ImportRequest> = object({
  encryptionScheme: ['encryptionScheme', optional(string())],
  factor: ['factor', optional(number())],
  users: ['users', optional(array(lazy(() => userSchema)))],
  validateDbConstraints: ['validateDbConstraints', optional(boolean())],
  eventInfo: ['eventInfo', optional(lazy(() => eventInfoSchema))],
});
