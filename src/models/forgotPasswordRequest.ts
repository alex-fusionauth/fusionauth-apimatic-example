/**
 * FusionAuth APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  dict,
  lazy,
  object,
  optional,
  Schema,
  string,
  unknown,
} from '../schema';
import { EventInfo, eventInfoSchema } from './eventInfo';

/** Forgot password request object. */
export interface ForgotPasswordRequest {
  applicationId?: string;
  changePasswordId?: string;
  loginId?: string;
  sendForgotPasswordEmail?: boolean;
  state?: Record<string, unknown>;
  email?: string;
  username?: string;
  /** Information about a user event (login, register, etc) that helps identify the source of the event (location, device type, OS, etc). */
  eventInfo?: EventInfo;
}

export const forgotPasswordRequestSchema: Schema<ForgotPasswordRequest> = object(
  {
    applicationId: ['applicationId', optional(string())],
    changePasswordId: ['changePasswordId', optional(string())],
    loginId: ['loginId', optional(string())],
    sendForgotPasswordEmail: ['sendForgotPasswordEmail', optional(boolean())],
    state: ['state', optional(dict(unknown()))],
    email: ['email', optional(string())],
    username: ['username', optional(string())],
    eventInfo: ['eventInfo', optional(lazy(() => eventInfoSchema))],
  }
);
